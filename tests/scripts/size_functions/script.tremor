use std;

{
    "kiB": std::math::round(std::size::kiB(event)),
    "MiB": std::math::round(std::size::MiB(event)),
    "GiB": std::math::round(std::size::GiB(event)),
    "TiB": std::math::round(std::size::TiB(event)),
    "PiB": std::math::round(std::size::PiB(event)),
    "EiB": std::math::round(std::size::EiB(event)),
}
