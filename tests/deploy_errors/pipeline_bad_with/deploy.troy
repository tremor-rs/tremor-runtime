define pipeline passthrough
args
  snot,
  badger,
with
  snot = "naughty badger"
pipeline
   select { "#{args.snot}": "#{args.badger}" } from in into out;
end;

deploy passthrough
with
  snot = "snot",
end;
