define pipeline passthrough
args
  snot,
  badger,
with
  snot = "naughty badger"
pipeline
   select { "#{args.snot}": "#{args.badger}" } from in into out;
end;

create instance passthrough
with
  snot = "snot",
end;
