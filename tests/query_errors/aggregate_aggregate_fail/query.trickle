mod m with
    aggregate ag of
        init ( ) =>
            0

        aggregate(st, x) =>
            1 * []

        mergein(st, x) =>
            st + x

        emit(st) =>
            st
    end
end;

select m::ag(event) from in into out;