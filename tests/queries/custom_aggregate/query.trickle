define tumbling window by_two
with
  size = 2
end;

aggregate collect_joined of
    init ( ) =>
        "not an empty string"

    aggregate(st, x) =>
        st + x

    mergein(st, x) =>
        st + x

    emit(st) =>
        st
end;

select collect_joined(event) from in[by_two] into out;
