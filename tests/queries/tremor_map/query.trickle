define script s
script
  let event.tremor.index_key = core::string::format("{}-{}", event.application, event.type);
  let event.tremor.instance = core::system::instance();
  event
end;

create script s;

select event from in into s;

select event from s into out;