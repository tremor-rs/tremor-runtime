define script logging
args
  seed = 10,
  name = "s0",
script
  match state of
    case null =>
      let state = {"count": args.seed}
    default =>
      let state.count = state.count + 1
  end;

  { "e": event, "#{args.name}": state.count }
end;