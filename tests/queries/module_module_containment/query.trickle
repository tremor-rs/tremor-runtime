mod m with
  const a = 2;
      fn add(v) with
      v - a
    end;
end;

define pipeline add
pipeline
  mod m with
    const a = 1;
    fn add(v) with
      v + a
    end;
  end;
  select m::add(event) from in into out;
end;

create pipeline add;
select event from in into add;
select event from add into out;