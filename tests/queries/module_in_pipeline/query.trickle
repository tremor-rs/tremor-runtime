mod m with
  define pipeline pass
  pipeline
    mod m with
      define pipeline pass
      pipeline
        select event from in into out
      end
    end;
    create pipeline pass from m::pass;
    select event from in into pass;
    select event from pass into out;
  end
end;

create pipeline pass from m::pass;
select event from in into pass;
select event from pass into out;