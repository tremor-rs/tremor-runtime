apiVersion: v1
kind: Service
metadata:
  labels:
    app: tremor-runtime
  name: tremor-runtime-service
  namespace: default
spec:
  ports:
  - port: 9898
    protocol: TCP
    targetPort: 9898
  selector:
    app: tremor-runtime
  type: ClusterIP
status:
  loadBalancer: {}
