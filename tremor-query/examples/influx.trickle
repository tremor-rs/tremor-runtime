create tumbling window `1secs`
 with
   #IOU constant functions
   #interval = datetime::with_seconds(5),
   interval = 1000000000
end;

create tumbling window `1secs`
 with
   #IOU constant functions
   #interval = datetime::with_seconds(5),
   interval = 1000000000
end;

select
  {
    "count": stats::count(),
    "group": event.tags.app
  }
from in[`1secs`]
where record::contains(event.tags, "app")
group by set(event.tags.app)
into out;
