define generic::wal operator wal
with
   dir = "./integration_testing/basic/query-wal.test/wal",
   read_count = 5,
   max_elements = 100,
end;
create operator wal;

select event from in into wal;
select event from wal into out;
