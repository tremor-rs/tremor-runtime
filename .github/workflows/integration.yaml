name: Integration tests

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  cli-test-harness:
    strategy:
      matrix:
        os:
          - ubuntu-latest
          #- windows-latest
          - macOS-latest
        include:
          - os: "macOS-latest"
            exclude_tests: "--excludes docker"
            rust_flags: "-C target-feature=+sse4.2"
          - os: "ubuntu-latest"
            exclude_tests: ""
            rust_flags: "-C target-feature=+avx,+avx2,+sse4.2"
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v1
      - name: Install deps
        if: matrix.os == 'ubuntu-latest'
        run: sudo apt-get -qy update && sudo apt-get install -y libssl-dev libssl1.1
      - uses: actions-rs/toolchain@v1
        with:
          override: true
          profile: minimal
      - uses: Swatinem/rust-cache@v1
      - name: Build test runner
        run: cargo build -p tremor-cli
      - name: Run Integration Tests
        run: cargo run -p tremor-cli -- test integration tremor-cli/tests ${{ matrix.exclude_tests }}
        env:
          TREMOR_PATH: "$GITHUB_WORKSPACE/tremor-script/lib"
          RUSTFLAGS: "${{ matrix.rust_flags }}"
      - name: Run API Tests
        run: cargo run -p tremor-cli -- test api tremor-cli/tests ${{ matrix.exclude_tests }}
        env:
          TREMOR_PATH: "$GITHUB_WORKSPACE/tremor-script/lib"
          RUSTFLAGS: "${{ matrix.rust_flags }}"
      - name: Run Unit Tests
        run: cargo run -p tremor-cli -- test unit tremor-cli/tests ${{ matrix.exclude_tests }}
        env:
          TREMOR_PATH: "$GITHUB_WORKSPACE/tremor-script/lib"
          RUSTFLAGS: "${{ matrix.rust_flags }}"
      - name: Run Command Tests
        run: cargo run -p tremor-cli -- test command tremor-cli/tests ${{ matrix.exclude_tests }}
        env:
          TREMOR_PATH: "$GITHUB_WORKSPACE/tremor-script/lib"
          RUSTFLAGS: "${{ matrix.rust_flags }}"
      - name: Upload error logs
        uses: actions/upload-artifact@v2
        if: failure()
        with:
          name: test-errors
          path: tremor-cli/tests/**/*.log
